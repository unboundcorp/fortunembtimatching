# 라이브에서 문제가 났을 때 (2026-09-09 대표님 지시 "라이브 나고 문제나면 대안을 마련해야지")

**당황해서 이것저것 고치지 마십시오. 순서가 있습니다.**
고치는 것보다 **되돌리는 것이 항상 빠르고 안전합니다.** 되돌린 다음에 원인을 찾습니다.

---

## 0. 먼저 무엇이 문제인지 1분 안에 봅니다

```bash
node ~/dev/harness-project\(coding\)/output/fortune/scripts/live.cjs
```

배포본에 대고 읽기만 합니다(아무것도 안 고칩니다). 15가지를 보고 사람 말로 알려줍니다 —
페이지가 열리는지 · 나가 있는 파일이 내 손의 것과 같은지 · **카카오 로그인이 켜져 있는지** ·
서버 창구 아홉 개 · 지금 test 키인지 live 키인지.

---

## 1. 갈래별 대응

| 증상 | 먼저 볼 것 | 대응 |
|---|---|---|
| **아무도 서비스에 못 들어온다** (동의 화면에서 안 넘어감) | `live.cjs` 의 `ready=` | `false` 면 Vercel 환경변수 `KAKAO_REST_API_KEY` 가 빠진 것입니다. 넣으면 즉시 풀립니다 |
| 화면이 하얗다 / 아무것도 안 그려진다 | `live.cjs` 의 JS 오류 줄 | **2번(되돌리기)** 으로 갑니다 |
| 결제는 됐는데 글이 안 열린다 | `/api/content` · `/api/interpret` 응답 | 손님께는 **[문의하기] → [결제했는데 결과가 안 보여요]** 를 안내합니다. 그 화면이 지금 권한 상태를 그 자리에서 보여줍니다. 영수증 번호로 [구매 내역 복원]도 됩니다 |
| 특정 화면만 깨졌다 | 어느 화면인지 | 급하지 않으면 고쳐서 올립니다. 손님이 계속 만나는 자리면 **2번** |
| 돈이 안 나가야 하는데 나갔다 / 반대 | `live.cjs` 의 '결제 키' 줄 | Vercel 환경변수 `TOSS_CLIENT_KEY` · `TOSS_SECRET_KEY` 를 **같은 쌍**으로 바꿉니다 |

---

## 2. 되돌리기 — 마지막으로 멀쩡했던 곳으로

```bash
cd ~/dev/harness-project\(coding\)/output/fortune
git log --oneline -10            # 어디로 되돌릴지 고른다
git revert --no-edit <되돌릴 커밋>   # 되돌리는 커밋을 새로 쌓는다 (기록이 남는다)
GIT_TERMINAL_PROMPT=0 git push
```

★ **`git reset --hard` 로 지우지 마십시오.** 되돌린 사실 자체가 기록으로 남아야
  다음에 "그때 무엇을 되돌렸나"를 볼 수 있습니다.
★ 푸시하면 Vercel 이 자동으로 다시 배포합니다(20~60초). 그 뒤 `live.cjs` 로 확인합니다.
★ Vercel 화면에서 **이전 배포를 Promote** 하는 길도 있습니다. 더 빠르지만 저장소와
  어긋나므로, 급한 불을 끈 뒤 반드시 위 `git revert` 로 저장소도 맞춰 두십시오.

---

## 3. 되돌려도 안 풀리는 것 — 되돌릴 수 없는 것들

**미리 알아 두셔야 할 것: 아래 셋은 코드를 되돌려도 안 돌아옵니다.**

| 무엇 | 왜 | 지금 상태 |
|---|---|---|
| 점수표·등급 선·프롬프트를 바꾸면 저장 열쇠가 달라져 만들어 둔 유료 글을 못 찾는다 | `cacheKeyOf` 가 payload 통째의 해시라, 재료가 한 글자만 달라져도 다른 글로 친다 | **2026-09-09에 안전망을 만들었습니다**(아래) — 같은 대상으로 만든 글을 되찾아 보여줍니다. 그래도 새 점수에 맞는 글을 다시 써 주지는 않습니다 |
| 기기 비우기 표식(`STATE_RESET_ID`)을 바꾸면 손님 기기의 프로필·기록이 지워진다 | 읽는 맨 앞에서 버리기 때문 | 판매 후 **절대 바꾸지 마십시오** |
| 서버에서 지운 자료 | 되살릴 백업이 따로 없다 | 지우기 전에 반드시 여쭙기 |

★ 이 셋은 "되돌리면 된다"가 통하지 않는 자리입니다. **올리기 전에 막는 수밖에 없습니다.**

### 이 안전망은 2026-09-09에 만들었습니다 (대표님 승인)

**"결제하신 글은 무슨 일이 있어도 열린다"** — 저장 열쇠가 안 맞고 남은 횟수도 없을 때,
그냥 거절하는 대신 **같은 상품·같은 대상으로 이미 만들어 둔 글**을 찾아 보여줍니다.
화면에는 "계산 방식이 바뀐 뒤라, 먼저 만들어 드린 풀이를 그대로 보여드려요"를 적습니다.

· 표식(`ai_cache.subject_key`)에는 **점수·등급·문구가 한 조각도 안 들어갑니다.** 그래서
  점수표를 고쳐도 살아 있습니다.
· 궁합은 **두 분 모두 + 사이**를 표식에 넣습니다. 남의 짝 글이 나가지 않게 하려는 것입니다.
· 되찾기는 새로 만들지 않고, 횟수를 안 쓰고, 돈이 안 나갑니다.
· 검사기 `checks/recover_pair.cjs`(28건)가 지킵니다.

★ 그래도 **점수표·문구를 고칠 때는 여전히 조심하십시오.** 이 장치는 *이미 만든 글을
  되찾아 주는 것*이지, 새 점수에 맞는 글을 다시 써 주는 것이 아닙니다.
★ **아직 실제 결제로 끝까지 밟아 보지는 못했습니다** — 표식과 배선은 검사로 확인했고,
  실제 DB에서 되찾아지는 것은 미검증입니다.

---

## 4. 남은 위험 (2026-09-09 확인)

**Supabase `user_sync` 표에 행 수준 보안(RLS)이 꺼져 있습니다.** 다른 표(orders·groups·
ai_cache 등)는 켜져 있는데 이 표만 꺼져 있습니다. 이 표에는 카카오로 로그인하신 분의
프로필·기록·저장한 모임이 들어 있습니다.

· **지금 당장 새는 것은 아닙니다** — 화면(fortune.html)은 Supabase 를 직접 부르지 않고
  anon 키가 어디에도 안 실려 있습니다(확인함). 서버 함수만 서비스 키로 접근합니다.
· 다만 anon 키가 한 번이라도 밖에 나가면 그 순간 **누구나 읽고 고칠 수 있는 표**입니다.
· 켜는 SQL: `ALTER TABLE "public"."user_sync" ENABLE ROW LEVEL SECURITY;`
  ★ **그냥 켜면 안 됩니다.** 정책 없이 켜면 접근이 통째로 막힐 수 있습니다.
    켜기 전에 서비스가 실제로 도는지 눌러 확인해야 합니다.
· **대표님이 정하실 일입니다.** 제가 임의로 켜지 않았습니다.

---

## 5. 손님께 알리기


문의는 서비스 안에서 받고 답합니다(시트가 아니라). `/fortune.html#unlock` 에 운영자 코드를
넣으면 **[더보기] → [설정]** 에 [문의 관리 (운영자)] 가 생깁니다. 거기서 단 답이
손님 화면 [내 문의 내역]에 바로 뜨고, 구글 시트에도 함께 적힙니다.
새 문의는 메일로 알림이 옵니다.
