# 유료 결제 과정 점검 — 2026-08-27

실제 사이트(www.inyeonjeom.kr)에서 브라우저로 직접 눌러가며 확인했습니다.
텔레그램 연결이 끊겨 보고를 못 보내는 동안 여기에 적어 둡니다.

---

## 가장 큰 것 — 돈 낸 사람이 남의 풀이를 받고 있었습니다

**증상.** 1975년생 남자(세훈, 화 기운)로 유료 사주 풀이를 열었더니,
1990년생 여자(가영, 금 기운)의 풀이가 그대로 왔습니다. 글은 이렇게 시작합니다.

> "**가영 님의** 사주는 여덟 글자 가운데 **쇠와 보석의 기운(금)이 네 개**나 자리해…"

세훈은 금이 아니라 화입니다. 이름도 사주도 남의 것입니다.

**원인.** `api/_lib/aiprompt.js` 의 캐시 열쇠 만드는 줄 하나입니다.

```js
JSON.stringify(payload, Object.keys(payload || {}).sort())
```

`JSON.stringify`의 두 번째 자리에 배열을 주면 그건 **정렬 순서가 아니라
"남길 칸 이름 목록"** 이고, 그 목록이 모든 깊이에 똑같이 적용됩니다.
`Object.keys`는 맨 위 칸 이름만 주므로, 한 겹만 들어가도 통째로 지워집니다.

사주 풀이 payload의 맨 위 칸은 여덟 개인데 글자값은 둘뿐이고 나머지는 다 객체라,
사람이 누구든 열쇠 재료가 늘 이 한 줄이었습니다.

```
{"daewoon":{},"dayMasterStrength":"strong","elements":{},"kind":"saju",
 "person":{},"pillars":{},"requested":{},"sipseong":{}}
```

**그래서 실제로 존재한 열쇠의 개수:**

| 상품 | 있어야 할 열쇠 수 | 실제 열쇠 수 |
|---|---|---|
| 사주 풀이 | 사람 수만큼 | **2개** (신강 / 신약) |
| 성격유형 풀이 | 사람 수만큼 | **1개** |
| 궁합 풀이 | 커플 수만큼 | **1개** |

성격유형 풀이와 궁합 풀이는 **먼저 만든 한 사람의 글이 그 뒤 모든 손님에게
그대로 나가는 상태**였습니다. 1,900원을 받고 남의 이름이 적힌 글을 주는 것입니다.

**고쳤습니다.** 깊이까지 내려가며 칸 이름을 정렬해 직접 쓰는 함수를 만들어 넣었습니다.
확인한 것:
- 서로 다른 두 사람 → 서로 다른 열쇠 ✔
- 같은 사람인데 칸 순서만 다름 → 같은 열쇠 ✔ (다시 열 때 공짜로 열리는 성질 유지)

**주의할 점 하나.** 열쇠 계산이 바뀌므로 **지금 저장돼 있는 글은 전부 못 찾게 됩니다.**
이미 한 번 만들어 본 세션은 다시 열 때 새로 써야 하는데, 횟수를 다 썼으면
"횟수를 다 쓰셨어요"로 막힙니다. 지금 `ai_usage`에 8줄뿐이고 전부 시험 세션이라
실제 손님 피해는 없습니다. 그 줄들을 지울지는 대표님이 정하실 일이라 손대지 않았습니다.

---

## 두 번째 — 개인정보처리방침에 적은 것과 코드가 달랐습니다 (고침)

처리방침과 위탁 표에 이렇게 적혀 있습니다.

> "닉네임과 생년월일·태어난 시각 원본은 AI에 전송하지 않습니다."
> "(닉네임·생년월일·태어난 시각 원본은 보내지 않음)"

그런데 실제로 쓰이는 창구(`/api/interpret`)는 둘 다 보내고 있습니다.

```js
person: { name: profile.name,                     // ← 닉네임
          birth: { year, month, day, time, ... } } // ← 생년월일·태어난 시각 원본
```

증거는 서버에 저장된 글 자체입니다 — "가영 님의 사주는…"으로 시작합니다.
AI가 이름을 알고 있었다는 뜻입니다.

**왜 이렇게 됐나.** 창구가 둘인데 한쪽만 고쳤기 때문입니다.
`/api/content`(예비 창구)는 `contentPayloadFrom()`으로 걸러 보내는데,
실제로 쓰이는 `/api/interpret`(흘려받기)는 걸러내지 않고 통째로 보냅니다.
{{a}}/{{b}} 사고와 정확히 같은 모양입니다.

**대표님 승인으로 ①을 골랐습니다 — 코드를 방침에 맞췄습니다.**
사주·성격유형 payload에서 닉네임과 생년월일·태어난 시각 원본을 뺐고,
이름 부를 자리는 `{{name}}`으로 받아 화면에서 채우도록 `namePlaceholders`를 넣었습니다.
시스템 지침 6번은 원래부터 "이름은 주어지지 않습니다"를 전제로 쓰여 있었으므로,
지침이 아니라 payload가 어긋나 있던 것이었습니다.
여덟 글자(pillars)는 그대로 보냅니다 — 풀이에 실제로 필요하고 위탁 표에도 적혀 있습니다.

---

## 고친 것들

### 결제하고 돌아오면 기다림 창이 안 떴습니다 — 고침

풀이를 새로 쓸 때 "보통 2분쯤 걸려요" 하는 창이 떠야 하는데, 코드에 이런 줄이 있었습니다.

> 이미 열려 있는 창이 있으면 → 기다림 창을 띄우지 않는다

결제하고 돌아오면 "결제가 완료되었어요" 창이 떠 있습니다. 그래서 그 자리에서 취소되는데,
이 함수는 딱 한 번만 불려서 **다시는 시도하지 않았습니다.**
즉 **돈을 낸 사람만 정확히 이 구멍에 빠졌습니다.**

실제 사이트에서 재 봤습니다 — **4분 24초 동안 기다림 창이 한 번도 안 떴습니다.**
결제한 사람이 보는 것: 창 닫음 → 본문 비어 있음 → 아무 설명 없음 → 2분 30초.

그 창이 닫힐 때까지 1초마다 다시 보도록 고쳤습니다.

### 이용권 조회가 2초씩 걸렸습니다 — 고침

세 번 재서 2.02 / 2.05 / 2.15초. 조회 네 개를 하나씩 차례로 기다리고 있었습니다
(결제 내역 → 만든 해석 → 테스트 허가 → 최근 주문, 각 0.5초).
서로의 결과를 안 쓰므로 한꺼번에 보내도록 고쳤습니다.

### 결제 단추가 화면 밖에 있었습니다 — 고침

결제 안내창을 열면 [카카오로 로그인하고 결제하기]가 **보이는 자리보다 841px 아래**에
있었습니다. 창 안쪽 글 1,652px, 보이는 높이 717px. 한 화면 반을 내려야 단추를 만납니다.
게다가 "카카오 로그인이 필요하다"는 것도 맨 끝에서야 알게 됩니다.

단추 줄을 창 아래에 붙여 두도록 고쳤습니다(글이 짧은 창은 그대로).
고친 뒤 다시 재니 창을 열자마자 단추가 보입니다.

### 결제 후 홈으로 가버리던 것 — 고침 확인

대표님 영수증 번호로 실제 사이트에서 확인했습니다. 홈 안 거치고 바로 들어갑니다.

```
0.0초  돌아옴      0.5초  영수증 확인
3.9초  이용권 조회  5.0초  AI 시작
8.0초  사주 풀이 화면 + 결제완료 창
```

---

## 확인 못 한 것

- **카카오 로그인 이후 → 토스 결제창 → 카드 승인** 구간을 제가 직접 통과하지 못했습니다.
  결제하려면 카카오 로그인이 필요한데 저는 계정이 없고, 대표님 계정을 쓰면 안 됩니다.
  이 구간은 대표님이 직접 한 번 해보셔야 합니다.
- 대표님 결제 기록(created 13:18:08 → paid 13:19:00, 52초)은 DB로만 확인했습니다.

## 남은 걱정거리 (안 고침)

- **해가 바뀌면 열쇠가 바뀝니다.** payload에 올해 연도와 나이가 들어 있어서,
  1월 1일이 되면 모두의 풀이가 다시 써져야 합니다. 단품 손님은 횟수에 막힙니다.
  고치는 방법은 있습니다(연도·나이를 열쇠 계산에서만 빼기). 다만 "산 풀이는 그 해 것으로
  영원히 고정"이라는 뜻이 되므로 대표님 판단이 필요해 손대지 않았습니다.

### 앞서 "앱 첫 화면 6.5초"라고 보고한 것 — 틀렸습니다

정정합니다. 실제로 재 보니 화면은 0.25초에 그려집니다.

```
HTML 받기 0.083초 · 파싱 끝 0.17초 · 첫 그림 0.25초 · 전부 로드 0.35초
```

제가 잰 6.5초는 "모든 통신이 끝날 때까지"였고, 그 안에서 이용권 조회 하나가
2.836초를 잡아먹고 있었습니다. 파일이 1.5MB라 느린 것이 아니라 그 조회 때문이었고,
그건 이 작업에서 이미 고쳤습니다.

## 시험하며 만든 것

- 시험 세션 3개에 3시간짜리 임시 이용권(`test_grants`)을 넣었습니다. 저절로 사라집니다.
- 새 사주 한 건을 AI에게 쓰게 했습니다(약 125원).
- 대표님 결제 기록은 **읽기만** 했습니다. 고치거나 지운 것 없습니다.

## 상태

커밋 `36b3d2b` 로 로컬에만 저장했습니다. **올리지 않았습니다.**
배포 전 검사: 부팅 0오류 · 문법 통과 · 화면 8개 통과 · 서버 파일 전수 import 정상.
